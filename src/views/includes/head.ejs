<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <!-- <link rel="shortcut icon" href="assets/favicon.ico"/> -->
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
  <title>AAC</title>
  <style type="text/css">
  
    /* SCROLLBAR */
    ::-webkit-scrollbar {
      width: 5px;
    }
    ::-webkit-scrollbar-thumb {
      -webkit-border-radius: 10px;
      border-radius: 10px;
      background: #e1e6e6; 
      -webkit-box-shadow: inset 0 0 6px rgb(100, 103, 103); 
      box-shadow: inset 0 0 6px rgb(100, 103, 103); 
    }

    /* HEADER */
    .header {
      background: linear-gradient(270deg, #1CA392 0%, #9CCCB2 43.23%, #1CA392 92.71%), #FFFFFF;
      height: 85px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .header .nav-link {
      color: white;
    }
    .header .nav-link.active {
      font-weight: 600;
    }
    .logo {
      display: flex;
      align-items: center;
    }
    .logo .school {
      font-size: 80px;
      color: white;
    }
    .logo .text {
      color: white;
      font-size: 40px;
      padding: 0 30px;
      font-style: italic;
      font-weight: 900;
    }

    /* GENERAL */
    .fluid-content {
      height: 100vh;
      font-family: 'Open Sans', sans-serif;
      color: #494949;
    }

    .disabled {
      color: #A8A8A8;
    }
    h5 {
      font-size: 18px;
      font-weight: 900;
    }
    p {
      font-size: 12px;
    }

    .card .card-body {
      text-align: center;
    }
    .card {
      width: 700px;
      margin: 0 auto;
      justify-content: center;
      padding: 35px;
      box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.25);
      border: none;
    }
    .card .card-body p{
      width: 500px;
      margin: 30px auto;
    }
    .btn:focus {
      outline: none;
      box-shadow: none;
    }

    /* BODY */
    .image-container {
      background: url(../assets/image/login-image.jpg) no-repeat;
      background-size: cover;
      width: 100%;
      height: 100%;
      opacity: 0.8;
    }
    .image-container .container-fluid {
      display: flex;
      align-items: center;
      margin-top: -85px;
      height: 100%;
    }
    .card .card-body .login-form {
      text-align: initial;
      width: 500px;
      margin: 0 auto;
    }
    .card .card-body .login-form .form-group {
      margin-bottom: 35px;
    }
    .card .card-body .login-form .buttons-login {
      display: flex;
      justify-content: space-between;
    }
    .buttons-login .btn.forgot-password-button {
      padding: 0;
      font-size: 11px;
      text-decoration: underline;
      color: #1CA392;
    }
    .buttons-login .btn.login-confirm {
      width: 150px;
      height: 35px;
      background-color: #1CA392;
      color: white;
      border-radius: 30px;
    }

    /* SCROLLBAR */
    ::-webkit-scrollbar {
      width: 5px;
    }
    ::-webkit-scrollbar-thumb {
      -webkit-border-radius: 10px;
      border-radius: 10px;
      background: #e1e6e6; 
      -webkit-box-shadow: inset 0 0 6px rgb(100, 103, 103); 
      box-shadow: inset 0 0 6px rgb(100, 103, 103); 
    }
  </style>
</head>
<body>